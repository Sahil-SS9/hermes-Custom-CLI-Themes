# Compatibility

These skins target Hermes Agent CLI skins.

The files include standard Hermes keys plus a small set of extended keys used by Sahil's local Hermes build:
- `response_text`
- `reasoning_border`
- `reasoning_text`

If a stock Hermes build ignores unknown keys, the skin still loads but reasoning and response text colours may not match the screenshots exactly.
